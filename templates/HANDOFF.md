# Handoff Notes — <TÊN PROJECT>

> File này GITIGNORE. Lưu brief cá nhân để chuyển giữa các session. Skill `/handoff` tự động cập nhật. Có thể chỉnh tay nếu cần.

---

## Phiên hiện tại — <YYYY-MM-DD HH:MM>

### Mục tiêu
<1 câu — đang giải quyết vấn đề gì>

### Đã xong
- <bullet — kèm path file>

### Đang dở
- <việc đang dở dang, dừng ở bước nào>
- File đang sửa: `path/to/file.ts:120` — <ghi chú>

### Quyết định đã chốt
- <decision A>: <rationale 1 câu>
- <decision B>: <rationale 1 câu>

### Constraint / điều cần nhớ
- <perf, compat, security, business rule>

### Đã thử nhưng KHÔNG work
- <approach X — bị reject vì lý do Y>

### Bước tiếp theo
1. <action cụ thể>
2. <action cụ thể>

---

## Lệnh hữu ích cho project này

```bash
# Build
<command>

# Test (khi nào dùng full vs watch)
<command>

# Run dev server
<command>

# Database migration / reset
<command>

# Reset state nếu rối
<command>
```

## Bookmark file

- **Entry point**: `<path>`
- **Config chính**: `<path>`
- **Schema DB**: `<path>`
- **Test fixtures**: `<path>`

---

## Phiên trước (archive — có thể xóa khi không cần)

<Mỗi lần `/handoff` ghi đè phần "Phiên hiện tại" ở trên. Nếu muốn giữ lịch sử, copy phần đó xuống đây.>

### <YYYY-MM-DD HH:MM>

<Nội dung phiên cũ.>
