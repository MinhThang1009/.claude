# Cấu hình plugins cần load vào ~/.claude/
# Format:
#   plugin-name          → load tất cả (agents + skills + commands)
#   plugin-name:agents   → chỉ load agents
#   plugin-name:skills   → chỉ load skills
#   plugin-name:commands → chỉ load commands
# Dòng bắt đầu bằng # là comment. Nếu file trống → load TẤT CẢ.

# === PR REVIEW TOOLKIT (agents + skills + commands) ===
pr-review-toolkit

# === FEATURE DEV ===
feature-dev

# === GIT WORKFLOW ===
commit-commands:skills

# === DEBUG ===
debug

# === SECURITY ===
security-guidance:agents

# === HOOKS ===
hookify

# === SESSION MANAGEMENT ===
session:skills

# === CODE QUALITY ===
code-simplifier:skills
frontend-design:skills
claude-md-management

# === TEST & PERFORMANCE ===
test-toolkit
performance

# === OPTIONAL: bỏ comment để enable ===
# mcp-server-dev           # Build MCP servers (remote HTTP, MCPB, UI widgets)
# mcp-builder              # Build MCP servers (FastMCP/SDK)
# plugin-dev               # Phát triển Claude Code plugins
# skill-creator            # Tạo/eval skills
# code-modernization       # Migrate legacy code
# documentation            # docs + Next.js agents
# claude-code-setup        # Recommend automations
# claude-api               # Claude API/SDK development
# webapp-testing           # Test web apps với Playwright
# web-artifacts-builder    # Build HTML artifacts (React, Tailwind)
# session-report           # Generate session reports
# code-review              # Standalone PR code review command
# docx                     # Word document creation/editing
# pdf                      # PDF manipulation
# pptx                     # PowerPoint creation/editing
# xlsx                     # Spreadsheet creation/editing
# theme-factory            # Apply themes to slides/docs/HTML (10 presets)
# doc-coauthoring          # Co-authoring workflow cho docs/proposals/specs
# explanatory-output-style # Giải thích implementation choices khi code
# learning-output-style    # Interactive learning mode tại decision points
# playwright               # Browser automation MCP (Microsoft) — cần npx
