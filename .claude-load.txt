# Cấu hình plugins load vào ~/.claude/
# Format:
#   plugin              → load agents + skills + commands
#   plugin:agents       → chỉ agents
#   plugin:skills       → chỉ skills
#   plugin:commands     → chỉ commands
#   !name               → exclude agent/skill theo tên (áp dụng mọi plugin)
# File trống → load TẤT CẢ plugins.

# Cơ chế junction đã NGHỈ HƯU — mọi plugin load qua marketplace (enabledPlugins
# trong settings.json). KHÔNG để file này trống: trống = load TẤT CẢ (CI dùng
# hành vi đó). Sentinel "none" dưới đây không match plugin nào → 0 junction.
# Muốn junction lại một plugin chưa enable: thay "none" bằng tên plugin.
none
