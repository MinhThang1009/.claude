// Commitlint config — enforce Conventional Commits (https://www.conventionalcommits.org).
// Used by .github/workflows/commitlint.yml. Goes at the repository root.
module.exports = {
  extends: ['@commitlint/config-conventional'],
};
