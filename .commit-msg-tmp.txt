refactor: move commitlint.config.js + cspell.json vào .github/ (clean root)

User feedback: "nhiều file ở root thế". Reduce root clutter bằng cách
move 2 tool configs vào .github/ — chỉ giữ ở root những file thực sự
phải ở đó (README, LICENSE, CHANGELOG, CLAUDE.md, settings.json).

Files moved:
- commitlint.config.js → .github/commitlint.config.js
- .cspell.json → .github/cspell.json

Workflow updates:
- commitlint.yml: thêm --config .github/commitlint.config.js flag
- spell.yml: thêm --config .github/cspell.json flag
- cspell.json: dictionaryDefinitions.path "dotclaude.txt" relative tới
  config location mới (.github/dictionaries/dotclaude.txt resolve tự động)

Files giữ ở root (essential):
- README.md, CLAUDE.md, CHANGELOG.md, LICENSE — GitHub conventions
- settings.json — Claude Code source of truth
- .gitignore, .editorconfig, .pre-commit-config.yaml — tool requires root
- .markdownlint-cli2.jsonc — tool auto-discover (move-able nhưng phá
  convention `npx markdownlint-cli2` local dev)

Visible files ở root giảm từ 7 → 5 (excluded HANDOFF.md gitignored).
